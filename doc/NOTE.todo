# TODO LIST FOR COMPONENT I
 - appfence ptraces zygote 
 - appfence gets zygote's fork() 
 - appfence forks, say process A. 
 - appfence ptraces process A 
 - appfence returns A's pid to zygote, as if it is forked by zygote 
 - appfence unattaches zyogte 
 - zygote exec process A, image changed but still being ptraced by appfence
